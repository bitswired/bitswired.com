import { Features, Head } from '@features/landing'

export default function Home() {
  return (
    <>
      <Head />
      <Features />

      {/* <Text as="body">Hello, from Stitches.</Text>

      <h1 className="text-4xl font-bold underline">Hello world!</h1>

      <Container gap="sm" css={{ height: '200px', alignItems: 'flex-start' }}>
        <Button variant="primary-solid" size="sm">
          SUBSCRIBE
        </Button>
        <Button variant="secondary-solid" size="md">
          SUBSCRIBE
        </Button>
        <Button variant="secondary-solid" size="lg">
          SUBSCRIBE
        </Button>
        <Button variant="primary-outlined" size="lg">
          SUBSCRIBE
        </Button>
        <Button variant="secondary-outlined" size="lg">
          SUBSCRIBE
        </Button>
      </Container> */}
    </>
  )
}
